{
  "name": "orc-monorepo",
  "license": "0BSD",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit || tsc -b",
    "build:cli": "node --import tsx scripts/build-cli.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "tsx": "^4.19.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
